$(document).ready(function() {
    $('#firstGradient').height($(window).height());
    $('#secondGradient').height($(window).height()/2)
    $('#thirdGradient').height($(window).height());
    $('#backgroundFlower').height($(document).height());
});